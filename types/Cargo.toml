[package]
name = "types"
version = "0.1.0"
authors = []
edition = "2018"

[dependencies]
bcs = "0.1.2"
serde = { version = "1.0.124", features = ["derive"] }
gdex-crypto = { path = "../crypto" }
gdex-crypto-derive = { path = "../crypto-derive", version = "0.0.2" }

[features]
batch = []
